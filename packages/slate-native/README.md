# @slate-terminal/native

Binding N-API oficial da Slate, compilado em Rust.

O pacote expõe renderização ANSI, Glow, ColorShift, teclado, mouse, paste
delimitado e mudanças de foco. Os artefatos nativos são gerados por plataforma
com `npm run native:build`.
